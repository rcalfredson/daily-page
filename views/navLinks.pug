a(
  href="#main-menu-toggle"
  id="main-menu-close"
  class="menu-close"
  aria-label=t('layout.closeMenu')
)
  span(class="sr-only")= t('layout.closeMenu')
  span(class="fa fa-close" aria-hidden="true")

ul
  li
    .home
      a.link(href=uiPath('/'))= t('nav.home')
  li
    .app
      a.link(href=uiPath('/rooms'))= t('nav.rooms')
  li
    .tags
      a.link(href='/tags')= t('nav.tags')
  li
    .archive
      a.link(href='/archive')= t('nav.archive')
  li
    .best-of
      a.link(href='/archive/best-of')= t('nav.bestOf')
  li
    .about
      a.link(href='/about')= t('nav.about')
  li
    .support
      a.link(href='/support')= t('nav.support')
  li
    .random
      a.link(href='/random')= t('nav.otherProjects')
