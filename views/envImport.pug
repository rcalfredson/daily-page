script.
  backendURL = "#{backendURL}"
  room_id = "#{room_id}"
  userId = "#{user.id}"
  block_id = "#{block_id}"
  initialTargetPeerId = "#{initialTargetPeerId}"
  canManageBlock = #{canManageBlock}
