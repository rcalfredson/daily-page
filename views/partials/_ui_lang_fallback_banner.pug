// views/partials/_ui_lang_fallback_banner.pug
-
  const requested = (uiLangFallbackFrom || '').toUpperCase();

div.ui-lang-fallback-banner(role="status" aria-live="polite" data-requested=uiLangFallbackFrom)
  span
    = t('layout.uiFallbackNotice', { requested, current: (uiLang || 'en').toUpperCase() })
  button.ui-lang-fallback-dismiss(type="button") ×
